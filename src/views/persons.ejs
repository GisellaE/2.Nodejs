<!DOCTYPE html>
<html>
<head>
  <title>Persons</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
  <div class="row justify-content-center">
    <h1 class="mb-4 text-success">Personas Mayores</h1>
    <ul class="list-group">
      <% filteredPersons.forEach(person => { %>
        <li class="list-group-item list-group-item-primary"><%= person.name %> - Edad: <%= person.age %> años</li>
      <% }); %>
    </ul>
  </div>
</body>
</html>